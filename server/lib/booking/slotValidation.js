/**
 * Shared slot capacity / kids-adults / overlap / break-rule validation.
 * Used by confirm-reserved (and available for book / future student API).
 */
export { assertBookableSlotForConfirm } from './domainInternals.js';
