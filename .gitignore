node_modules
.env
client/.env

# Google service account keys (never commit)
*-service-account*.json
*service-account*.json
*credentials*.json
student-admin-*.json

# Temp / askpass
.tmp_askpass.cmd

# Separate GAS project (clone: github.com/khaCey/calendarAPI → calendarAPI/; deploy via clasp or Apps Script)
calendarAPI/

# Migration data CSVs
migration-data/**/*.csv
