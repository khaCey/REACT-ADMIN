node_modules
.env
client/.env

# Google service account keys (never commit)
*-service-account*.json
*service-account*.json
*credentials*.json
student-admin-*.json

# Temp / askpass
.tmp_askpass.cmd

# Separate GAS project (local copy only; deploy from Apps Script)
Calendar API/

# Migration data CSVs
migration-data/**/*.csv
